default
	package.name			Samples
	website.name			SimpleWebsite
	all.servers				${web.servers}
	deployment.root			${root.drive}\sites
	http.port				80
	https.port				443

local
	web.servers				localhost 
	root.drive				c:
	http.port				9000
	https.port				9001

staging
	web.servers				stagingweb
	root.drive				e:
	
live
	web.servers				liveweb1;liveweb2 
	root.drive				g:

